defmodule DemoShopWeb do
  @moduledoc """
  DemoShopWeb uses PhireFlightWeb infrastructure.
  """
  # For simplicity, we use PhireFlightWeb directly
  # Controllers can use: use PhireFlightWeb, :controller
  # HTML modules can use: use PhireFlightWeb, :html
end

